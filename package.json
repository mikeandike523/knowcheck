{
  "scripts": {
    "link-code": "bash link-code.sh",
    "link-projects": "bash link-projects.sh",
    "dev": "concurrently --kill-others-on-fail \"pnpm run backend:dev\" \"pnpm run frontend:dev\"",
    "backend:dev": "cd backend && export GOOGLE_APPLICATION_CREDENTIALS=\"$(pwd)/service-account-key.json\" && pnpm run firebase emulators:start --only functions",
    "frontend:dev": "cd frontend && pnpm run dev"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.3.2"
  }
}
